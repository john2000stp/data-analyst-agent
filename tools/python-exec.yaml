name: python-exec
description: Execute Python code for data analysis, transformations, and visualizations
input_schema:
  type: object
  properties:
    code:
      type: string
      description: Python code to execute
  required:
    - code
